// Shared by the server-only session module and the proxy (which must stay a lightweight,
// dependency-free file: see AGENTS.md and the Next.js Proxy guide).
export const SESSION_COOKIE_NAME = "mercado_session";
